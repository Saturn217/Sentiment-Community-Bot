services:
  - type: web
    name: sentiment-bot
    runtime: node
    buildCommand: npm install
    startCommand: node src/bot.js
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /